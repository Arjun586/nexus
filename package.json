{
  "name": "nexus",
  "private": true,
  "scripts": {
    "dev": "concurrently --names \"BACKEND,FRONTEND\" --prefix-colors \"green,yellow\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm --prefix backend run build",
    "start": "npm --prefix backend start"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}